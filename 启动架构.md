对，**SAM 3 和 MoGe-2 不需要像 API 一样手动常驻启动**。

在你这个项目里，它们属于：

```text
短生命周期模型子进程
```

平时不运行。只有主 Pipeline Worker 执行到对应阶段时，才自动启动；推理完成、产物写盘后，模型进程自动退出。项目架构本身就是 `FastAPI + 常驻 Pipeline Worker + 短生命周期 SAM/MoGe 子进程`。

# 实际调用关系

```text
用户调用 API
    │
    ▼
SQLite 中任务变成 QUEUED
    │
    ▼
常驻 Pipeline Worker 领取任务
    │
    ▼
Orchestrator 按阶段执行
    │
    ├─ build_sam_tasks
    │
    ├─ sam_segment
    │      └─ 自动启动 sam3_worker.py
    │
    ├─ mask_postprocess
    │
    ├─ moge_estimate
    │      └─ 自动启动 moge2_worker.py
    │
    ├─ pixel_align
    ├─ ground_solve
    ├─ object_solve
    └─ export
```

Pipeline 的实际阶段顺序已经定义为：

```text
build_sam_tasks
sam_segment
mask_postprocess
moge_estimate
pixel_align
ground_solve
object_solve
group_solve
export
evaluate
```



---

# 1. 你手动启动的是主 Worker

你运行：

```bash
bash scripts/start_worker.sh
```

本质上启动的是：

```bash
/home/ai3d/envs/scene-core/bin/python \
  -m scene_spatial.worker.main
```

它会一直轮询数据库：

```python
while True:
    task = claim_next_task()

    if task is None:
        sleep(2)
        continue

    orchestrator.run(task)
```

它不直接加载 SAM 或 MoGe，只负责：

```text
领取任务
维护任务状态
执行阶段
启动模型子进程
检查模型结果
继续后续空间算法
```

第一阶段当前就是单个常驻 Pipeline Worker。

---

# 2. 执行到 SAM 阶段时发生什么

进入：

```text
sam_segment
```

主 Worker 会先创建请求文件：

```text
outputs/task_xxx/stages/sam_request_attempt_1.json
```

内容大致是：

```json
{
  "schemaVersion": "1.0",
  "taskId": "task_xxx",
  "imagePath": ".../input/sam_full.png",
  "tasksPath": ".../semantic/sam_tasks.json",
  "outputDir": ".../detection/raw",
  "device": "cuda:0",
  "dtype": "bfloat16"
}
```

然后主 Worker 通过 Python 的 `subprocess.run()` 自动执行：

```bash
/home/ai3d/envs/scene-sam3/bin/python \
  /home/ai3d/src/scene-spatial-poc/model_workers/sam3_worker.py \
  --request /.../sam_request_attempt_1.json \
  --response /.../sam_response_attempt_1.json
```

这个命令不是你手动执行，是主 Worker 通过：

```text
src/scene_spatial/infrastructure/subprocess_runner.py
```

启动的。

项目已经规定真实 SAM 模式使用：

```text
<SCENE_SAM_PYTHON>
<SCENE_SAM_WORKER>
--request
--response
```



当前 `.env.server` 提供：

```env
SCENE_SAM_PYTHON=/home/ai3d/envs/scene-sam3/bin/python
SCENE_SAM_WORKER=/home/ai3d/src/scene-spatial-poc/model_workers/sam3_worker.py
SCENE_SAM_CHECKPOINT=/home/ai3d/models/scene-spatial/sam3/sam3.pt
```

于是 `sam3_worker.py` 会：

```text
启动进程
→ 读取 request JSON
→ 加载 sam3.pt
→ 读取 sam_tasks.json
→ 执行所有 prompt
→ 保存 Mask
→ 保存 detections.json
→ 写 response JSON
→ 进程退出
```

你独立测试时看到的：

```json
{
  "status": "completed",
  "model": "sam3",
  "detectionsPath": ".../detections.json",
  "maskDir": ".../masks",
  "durationMs": 10221,
  "peakGpuMemoryMiB": 5087
}
```

就是主 Worker 将来自动等待和读取的返回结果。

---

# 3. SAM 完成以后，主 Worker 怎么继续

`subprocess.run()` 默认会等待子进程结束。

所以执行关系相当于：

```python
run_sam_subprocess()
read_sam_response()
verify_detections_exist()
continue_pipeline()
```

SAM 进程退出以后：

```text
SAM 模型释放
PyTorch 进程释放
SAM 占用的显存回收
```

然后主 Worker继续执行：

```text
mask_postprocess
```

处理 SAM 生成的 Mask。

---

# 4. 执行到 MoGe 阶段时发生什么

主 Worker执行到：

```text
moge_estimate
```

会创建：

```text
outputs/task_xxx/stages/moge_request_attempt_1.json
```

内容大致是：

```json
{
  "schemaVersion": "1.0",
  "taskId": "task_xxx",
  "imagePath": ".../input/moge_input.png",
  "outputDir": ".../geometry",
  "device": "cuda:0",
  "dtype": "float32"
}
```

然后自动运行：

```bash
/home/ai3d/envs/scene-moge2/bin/python \
  /home/ai3d/src/scene-spatial-poc/model_workers/moge2_worker.py \
  --request /.../moge_request_attempt_1.json \
  --response /.../moge_response_attempt_1.json
```

`moge2_worker.py` 内部执行：

```text
启动进程
→ 加载 model.pt
→ 读取 moge_input.png
→ model.infer()
→ 保存 points.npy
→ 保存 depth.npy
→ 保存 validity.npy
→ 计算 normals.npy
→ 保存 camera.json
→ 写 response JSON
→ 退出
```

MoGe 协议要求返回：

```text
pointsPath
depthPath
normalsPath
validityPath
cameraPath
durationMs
peakGpuMemoryMiB
```



MoGe 退出后，显存同样释放，主 Worker继续：

```text
pixel_align
ground_solve
object_solve
```

---

# 5. `SCENE_FAKE_MODELS` 控制的是什么

当配置为：

```env
SCENE_FAKE_MODELS=true
```

主 Worker启动模型脚本时，会附加：

```bash
--fake
```

也就是：

```bash
sam3_worker.py --request ... --response ... --fake
moge2_worker.py --request ... --response ... --fake
```

于是代码执行：

```python
if args.fake:
    run_fake(...)
else:
    run_real(...)
```

现在你已经改成：

```env
SCENE_FAKE_MODELS=false
```

主 Worker不会传 `--fake`，因此自动进入：

```python
run_real(...)
```

这就是切换真实模型的开关。

---

# 6. 为什么不把 SAM 和 MoGe 做成常驻服务

当前设计有几个明显好处。

## 环境隔离

三个进程分别使用：

```text
scene-core
scene-sam3
scene-moge2
```

SAM 使用 Python 3.12、Torch 2.10；MoGe 使用 Python 3.10、Torch 2.6。它们不需要强行安装到一个环境里。

## 显存能够释放

如果做成长驻服务：

```text
SAM 权重长期占一份显存
MoGe 权重长期占一份显存
```

当前方式是：

```text
运行 SAM
→ SAM 退出释放显存
→ 运行 MoGe
→ MoGe 退出释放显存
```

更适合当前单 GPU、低并发 PoC。

## 模型崩溃不拖垮主 Worker

SAM 或 MoGe 报错时，通常只是子进程返回非零状态。

主 Worker还在，可以：

```text
记录 stderr
标记当前 stage FAILED
保存 request / response
后续重跑
```

不会把 API 一起弄崩。

## 调试方便

每次任务都会留下：

```text
stages/sam_request_attempt_1.json
stages/sam_response_attempt_1.json
logs/sam.stdout.log
logs/sam.stderr.log

stages/moge_request_attempt_1.json
stages/moge_response_attempt_1.json
logs/moge.stdout.log
logs/moge.stderr.log
```

能清楚知道：

```text
主 Worker传了什么
模型返回了什么
在哪一步报错
用了多少时间和显存
```

---

# 7. 你实际只需要启动两个进程

```text
终端 1：FastAPI
终端 2：Pipeline Worker
```

SAM 和 MoGe 的生命周期是：

```text
平时：不存在

任务执行到 SAM：
    sam3_worker.py 进程出现
    推理完成后消失

任务执行到 MoGe：
    moge2_worker.py 进程出现
    推理完成后消失
```

任务运行期间，可以用下面的命令观察：

```bash
watch -n 1 \
"pgrep -af 'sam3_worker.py|moge2_worker.py|scene_spatial.worker.main'"
```

运行 SAM 时会看到：

```text
scene_spatial.worker.main
sam3_worker.py --request ... --response ...
```

SAM 完成以后，`sam3_worker.py` 消失。

运行到 MoGe 时会看到：

```text
scene_spatial.worker.main
moge2_worker.py --request ... --response ...
```

所以可以把它理解为：

> Pipeline Worker 是总导演，SAM 3 和 MoGe-2 是需要出场时才被导演临时叫来的演员。
