有，而且我查下来后结论比较清楚：

> **没有一个开源项目能直接把你现在的 `SAM Mask + MoGe Point Map` 一次性变成可靠的园区三维布局。最适合你的方案不是整套替换，而是针对 5～8 阶段分别引入成熟算法。**

你现在的前半段已经有：

```text
SAM3：对象和区域 Mask
MoGe-2：Point Map、Depth、Normal、Camera
```

接下来最稳的技术路线应该是：

```text
像素映射：OpenCV / Kornia
地面求解：Open3D RANSAC + 法线约束
对象求解：Mask 点云清洗 + 类别专用几何算法
方向辅助：DeepLSD / 消失点
区域求解：OpenCV + Shapely + scikit-image
组关系：HDBSCAN + PCA + RANSAC
可选大模型校验：DetAny3D / Cube R-CNN
```

---

# 一、阶段 5：`pixel_align`

## 最适合你的开源库

### 1. OpenCV

OpenCV 已经有完整的：

```text
resize
remap
warpAffine
warpPerspective
```

以及像素插值和几何变换能力。([docs.opencv.org][1])

你当前 SAM 和 MoGe 本质上来自同一张原始图片，只是可能经历：

```text
缩放
补边
裁剪
切片
```

因此正常情况下，**不需要大模型进行对齐**。

应该在图片预处理时保存一个明确变换：

```json
{
  "originalSize": [1914, 870],
  "samSize": [1914, 870],
  "mogeSize": [1914, 870],
  "scaleX": 1.0,
  "scaleY": 1.0,
  "offsetX": 0,
  "offsetY": 0,
  "crop": null,
  "padding": null
}
```

然后把 SAM 像素确定性地映射到 MoGe：

```python
x_moge = x_sam * scale_x + offset_x
y_moge = y_sam * scale_y + offset_y
```

## 2. Kornia

Kornia 是面向 Python/PyTorch 的几何视觉库，支持仿射、单应性、图像配准，也集成了 LoFTR 和 LightGlue。([GitHub][2])

适合以下情况：

```text
SAM 和 MoGe 使用了不同裁剪
预处理元数据丢失
需要根据图像内容反推单应变换
```

## 3. LightGlue

LightGlue 是稀疏特征匹配模型，可以在两张图之间建立对应点，再通过 RANSAC 求单应矩阵。([GitHub][3])

但你的项目里它只应作为：

```text
pixel alignment fallback
```

而不是正常主流程。

## 推荐结论

当前 `pixel_align` 不需要更换为大模型，应改成：

```text
预处理阶段记录 Transform
→ pixel_align 读取 Transform
→ 生成 mask_on_depth.png 验证
```

除非 SAM 和 MoGe 的图像真的发生未知变形，才启用 Kornia/LightGlue。

---

# 二、阶段 6：`ground_solve`

## 1. Open3D `segment_plane`

这是最适合当前项目的基础算法。

Open3D 提供：

```python
point_cloud.segment_plane(
    distance_threshold,
    ransac_n,
    num_iterations
)
```

使用 RANSAC 从点云中寻找支持点最多的平面；同时提供 DBSCAN 聚类和包围盒等点云操作。([Open3D][4])

但你不能直接对全场景点云跑“最大平面”，否则可能找到：

```text
建筑立面
屋顶
大块玻璃幕墙
```

更合适的是：

```text
ground / road / vegetation Mask
→ 提取对应 Point Map
→ validity 过滤
→ 法线朝向过滤
→ 深度离群过滤
→ RANSAC 平面
```

建议约束：

```python
candidate = (
    semantic_ground_mask
    & validity
    & finite_points
    & normal_close_to_expected_up
)
```

然后对候选点运行 Open3D。

---

## 2. Patchwork++

Patchwork++ 是开源的鲁棒地面分割算法，目标是从 3D LiDAR 点云中快速、自适应地识别地面。官方仓库提供 C++ 和 Python 支持。([GitHub][5])

它比单次全局 RANSAC更适合：

```text
地面有坡度
地形不完全平坦
点云范围很大
不同距离区域噪声不同
```

但要注意：

```text
Patchwork++ 原始设计输入是 LiDAR 扫描
你的 Point Map 是单图伪点云
```

因此不能直接原样套用，应该借鉴它的思想：

```text
按距离分环
按扇区切分
局部拟合地面
再合并局部地面模型
```

对于你的园区图，我认为：

```text
第一版：Open3D 受约束 RANSAC
第二版：参考 Patchwork++ 做分区地面拟合
```

---

## 3. PlaneRCNN / PlaneTR

PlaneRCNN 和 PlaneTR 这类项目可以从单张 RGB 图像恢复平面分割和三维平面参数；PlaneTR 官方实现还结合了线段结构信息。([GitHub][6])

它们可以作为：

```text
地面平面候选交叉验证
建筑立面平面识别
```

但不建议直接替换 MoGe + Open3D，因为：

```text
模型训练域通常偏室内和规则平面环境
你的输入是园区编辑器渲染图
已有 MoGe Point Map 可直接使用
```

## 推荐结论

`ground_solve` 最现实的升级组合：

```text
SAM ground/road/vegetation 区域
        ↓
MoGe points + normals + validity
        ↓
Open3D DBSCAN 清理孤立点
        ↓
法线约束
        ↓
Open3D RANSAC segment_plane
        ↓
按距离分区重复拟合
        ↓
输出平面、内点率、残差和置信度
```

---

# 三、阶段 7：`object_solve`

这一块现成算法最多，但也最容易选错。

## 方案 A：继续使用 Mask + Point Map，但换成成熟几何算法

这是我最推荐的近期方案。

Open3D 已经提供：

```text
统计离群点删除
半径离群点删除
DBSCAN
平面分割
AABB / OBB
点云裁剪
```

([Open3D][4])

但不能继续用：

```text
整个 Mask 的全部三维点
→ 一次 PCA / OBB
→ 所有类别统一输出 width height length yaw
```

应该拆分类别。

---

## 建筑：Open3D + OpenCV + DeepLSD

### 推荐算法链

```text
建筑 Mask
→ 提取 Point Map
→ DBSCAN 保留主点簇
→ 中位数/MAD 深度过滤
→ 去除前后景点
→ 投影到地面坐标系
→ 计算二维 footprint
→ OpenCV minAreaRect
→ 高度沿 ground normal 计算
```

OpenCV 提供 `minAreaRect` 和 `approxPolyDP`，适合从二维轮廓或投影点求最小旋转矩形和简化多边形。([docs.opencv.org][7])

### 建筑朝向：DeepLSD

DeepLSD 是开源线段检测与优化模型，官方实现也包含消失点估计能力。([GitHub][8])

它特别适合你的编辑器建筑图：

```text
检测建筑立面水平线和竖线
→ 聚类消失点
→ 获取曼哈顿主方向
→ 修正建筑 yaw
```

这样建筑朝向不再只依赖噪声很大的 MoGe 点云 PCA。

建议组合：

```text
点云 footprint yaw
+
图像消失点 yaw
```

当二者接近时提高 `rotationConfidence`，差异很大时标记人工复核。

---

## 道路和绿地：Shapely + scikit-image

道路和绿化区不适合 OBB。

### 区域边界

使用：

```text
Mask
→ findContours / find_contours
→ approxPolyDP
→ 投影到地面
→ Shapely Polygon
→ simplify / union / buffer
```

scikit-image 的 `regionprops` 和 `find_contours` 可以计算区域属性、提取边界；Shapely 适合多边形合并和简化。([scikit-image.org][9])

### 道路中心线

使用：

```python
skimage.morphology.skeletonize(road_mask)
```

`skeletonize` 会通过细化将二值区域压缩成单像素宽骨架，很适合从道路 Mask 提取中心线拓扑。([scikit-image.org][10])

然后：

```text
骨架像素
→ 图结构
→ 删除短分支
→ 投影至地面
→ 输出中心线 polyline
```

这样道路输出不再是：

```text
一个巨大旋转矩形
```

而是：

```json
{
  "polygons": [],
  "centerlines": [],
  "junctions": [],
  "averageWidth": 0
}
```

---

## 路灯：类别先验几何

路灯不需要大模型解算 OBB。

直接用规则往往更可靠：

```text
Mask 最低点
→ 获得地面接触点
→ Up 固定为地面法线
→ Mask 内沿 Up 方向求高度
→ 水平尺寸使用鲁棒分位数
→ yaw 不重要
```

生成：

```text
圆柱杆
+
顶部小盒
```

比自由 OBB 更适合。

---

# 四、可直接输出三维盒子的大模型

## 1. DetAny3D

这是目前和你的需求最接近的开源大模型之一。

DetAny3D 官方定义是：

```text
promptable
monocular
open-vocabulary
3D detection
arbitrary camera configurations
```

也就是从单张图像根据文本提示预测开放类别的三维对象框。官方已发布代码和权重，但其仓库仍注明推理简化、数据转换和自定义训练教程仍在完善。([GitHub][11])

它可以用于你的：

```text
object_solve 第二路候选
```

推荐方式不是完全替换，而是：

```text
SAM + MoGe 几何盒子
DetAny3D 预测盒子
        ↓
比较中心、尺寸和朝向
        ↓
一致则提升置信度
不一致则 needs_review
```

### 优点

```text
直接预测 3D box
开放类别
单图输入
支持不同相机
```

### 风险

```text
园区建筑和细长路灯未必是其强项
当前集成流程仍偏研究代码
输出尺度和你的 MoGe 坐标系需要对齐
```

我会把它列为第一优先的模型验证对象。

---

## 2. WildDet3D

WildDet3D 是面向“in-the-wild”单目可提示三维检测的开源项目，并发布了带人工验证 3D 框的大规模数据。([GitHub][12])

它更适合：

```text
研究和训练数据参考
评估开放类别单目 3D 检测能力
后续做领域微调
```

但对于你现在立即修复园区布局，不如先测 DetAny3D 直接。

---

## 3. Cube R-CNN / Omni3D

Meta 的 Omni3D 提供 Cube R-CNN，从单图预测对象三维位置、尺寸和朝向。Omni3D 汇集了 234,000 张图像、300 多万个实例和 98 个类别，目标是跨相机和场景类型泛化。([arXiv][13])

它可以作为成熟基线：

```text
单图
→ 2D detection
→ camera-aware 3D cuboid
```

但问题是：

```text
类别仍受训练集限制
不是开放词汇
对建筑群、道路和区域几何帮助有限
代码栈基于 Detectron2，集成成本较高
```

适合作为 benchmark，不一定适合成为主方案。

---

## 4. SAM 3D Objects

Meta 的 SAM 3D Objects 可以从单张图像恢复对象的完整三维形状、纹理和布局。([GitHub][14])

但它更偏：

```text
单个前景对象的三维资产重建
```

而不是：

```text
几十栋建筑、道路、绿地、路灯的准确城市布局求解
```

公开问题中也有人报告多对象空间关系异常，因此不应直接当作你的 `object_solve` 替代品。([GitHub][15])

更适合后续：

```text
已经确定对象落点后
→ 为特定对象生成三维资产
```

不是现在这四个空间求解阶段的首选。

---

# 五、阶段 8：`group_solve`

这一阶段其实不需要大模型，成熟统计几何算法更适合。

## 1. HDBSCAN

HDBSCAN 可以处理：

```text
不同密度的对象群
噪声点
未知组数量
```

相较固定 `eps` 的 DBSCAN，它在不同密度聚类上更鲁棒，主要参数是 `min_cluster_size`。([GitHub][16])

适合：

```text
building cluster
street_light along_path 的初步分组
处理背景离群对象
```

流程：

```text
高置信度对象中心
→ 转换到地面二维坐标
→ 按类别分别运行 HDBSCAN
→ -1 标签作为噪声
```

---

## 2. PCA 求组主方向

每个组内中心点：

```text
center positions
→ PCA
→ principal axis
```

得到：

```text
建筑群主方向
道路沿线方向
阵列大致朝向
```

---

## 3. RANSAC 行列拟合

对一组同类对象：

```text
二维中心点
→ RANSAC 拟合直线
→ 提取一行
→ 删除内点
→ 继续拟合下一行
```

这比当前可能存在的“所有对象互相连线”合理得多。

---

## 4. 行列和网格识别

对于明确规则阵列：

```text
PCA 得到两个主轴
→ 对对象中心投影到 U/V 轴
→ 一维聚类
→ 得到 rowId / columnId
→ 计算平均间距及方差
```

只有满足以下条件才输出 `grid`：

```text
行数 ≥ 2
列数 ≥ 2
组内对象数足够
行间距方差低
列间距方差低
朝向差异低
```

否则只输出：

```text
cluster
row
along_path
```

---

## 5. 正确的组关系图

不要再画全连接蜘蛛网。

预览图应该只画：

```text
对象中心点
组边界凸包
PCA 主方向
最近邻关系
行列编号
```

连接边只允许：

```text
同组
同类别
最近邻
距离不超过组内中位距离的某个倍数
```

---

# 六、ConceptGraphs、OpenMask3D、Open3DSG 能不能用？

这些项目值得关注，但不适合你当前的单图第一阶段。

OpenMask3D 明确要求：

```text
场景点云
+
带位姿的 RGB-D 帧
```

([GitHub][17])

ConceptGraphs 也主要构建基于多帧分割结果的对象级三维地图。([GitHub][18])

Open3DSG 则以点云和现有三维场景数据为基础生成开放词汇场景图。([GitHub][19])

它们适合未来版本：

```text
多张图片 / 视频
→ 相机位姿
→ 融合 RGB-D
→ 真实三维对象图
```

不适合直接拿一张 MoGe Point Map 就替换你现在的 `group_solve`。

---

# 七、最终推荐技术组合

## 当前第一阶段最实际的组合

| 阶段                 | 推荐方案                                               | 是否需要新模型 |
| ------------------ | -------------------------------------------------- | ------- |
| pixel_align        | OpenCV 变换矩阵，Kornia/LightGlue 仅兜底                   | 否       |
| ground_solve       | Open3D 受约束 RANSAC，参考 Patchwork++ 分区思想              | 否       |
| building solve     | Open3D 清洗 + 地面投影 + OpenCV minAreaRect + DeepLSD 朝向 | 可选      |
| road solve         | Mask contour + Shapely Polygon + skeletonize 中心线   | 否       |
| vegetation solve   | MultiPolygon 投影和简化                                 | 否       |
| street-light solve | 地面锚点 + Up 方向类别先验                                   | 否       |
| object 3D box 校验   | DetAny3D                                           | 是，可选    |
| group solve        | HDBSCAN + PCA + RANSAC + 一维行列聚类                    | 否       |

---

# 八、我认为你最值得测试的三个开源项目

## 第一名：Open3D

不是因为它最“AI”，而是它正好补你现在最弱的一段：

```text
点云清洗
平面拟合
聚类
坐标系
包围盒
可视化
```

你项目已经有 Open3D 依赖，可以直接重构现有空间求解。

## 第二名：DetAny3D

用于评估：

```text
它预测的建筑 3D cuboid
是否比当前 Mask + Point Map OBB 稳定
```

先独立部署做 5～10 张图片对比，不要马上接入主 Pipeline。

## 第三名：DeepLSD

用于修正建筑朝向和相机曼哈顿方向。

它不会解决位置和尺寸，但能明显改善：

```text
建筑 yaw 乱转
同组建筑方向不一致
```

---

# 九、明确不建议直接采用的方案

暂时不要直接把以下系统接进主链：

### OpenPCDet / MMDetection3D

它们非常成熟，但大部分模型面向 LiDAR、自动驾驶或特定点云数据集；MMDetection3D 虽支持相机和单目方法，但仍需要匹配训练类别和数据格式。([GitHub][20])

### Patchwork++ 直接替换 ground_solve

它是 LiDAR 地面分割器，需要针对单图 Point Map 改造。

### SAM 3D Objects 直接恢复整座园区

它更适合对象资产生成，不是可靠的城市布局求解器。

### ConceptGraphs / OpenMask3D

它们需要多帧、相机位姿或 RGB-D 重建，不符合当前单图输入。

---

# 十、落到你项目里的建议架构

不要再保留一个通用 `object_solve` 处理所有类别，建议拆成：

```text
spatial/
├── alignment/
│   └── pixel_transform.py
├── ground/
│   ├── candidate_selector.py
│   ├── ransac_plane.py
│   └── local_ground.py
├── solvers/
│   ├── building_solver.py
│   ├── road_solver.py
│   ├── vegetation_solver.py
│   └── street_light_solver.py
├── grouping/
│   ├── cluster_solver.py
│   ├── row_solver.py
│   ├── grid_solver.py
│   └── along_path_solver.py
└── quality/
    ├── geometry_validator.py
    └── reprojection_validator.py
```

总体流程调整为：

```text
pixel_align
    ↓
ground_solve
    ↓
building / road / vegetation / street_light 分别求解
    ↓
geometry validation
    ↓
只让通过校验的对象进入 group_solve
    ↓
HDBSCAN + PCA + RANSAC
    ↓
reprojection validation
    ↓
RefinedSpatialSceneObservation
```

**最值得优先落地的不是新大模型，而是 Open3D + OpenCV + Shapely + scikit-image + HDBSCAN 这套类别专用几何链。DetAny3D 适合作为并行验证器，用于判断当前建筑三维框是否离谱。**

[1]: https://docs.opencv.org/4.x/da/d54/group__imgproc__transform.html?utm_source=chatgpt.com "OpenCV: Geometric Image Transformations"
[2]: https://github.com/kornia/kornia?utm_source=chatgpt.com "GitHub - kornia/kornia: 🐍 Geometric Computer Vision Library for Spatial AI · GitHub"
[3]: https://github.com/cvg/LightGlue?utm_source=chatgpt.com "GitHub - cvg/LightGlue: LightGlue: Local Feature Matching at Light Speed (ICCV 2023)"
[4]: https://www.open3d.org/docs/latest/tutorial/Basic/pointcloud.html?utm_source=chatgpt.com "Point Cloud — Open3D latest (664eff5) documentation"
[5]: https://github.com/url-kaist/patchwork-plusplus?utm_source=chatgpt.com "GitHub - url-kaist/patchwork-plusplus: Patchwork++: Fast and robust ground segmentation method for 3D LiDAR scans. @ IROS'22 · GitHub"
[6]: https://github.com/IceTTTb/PlaneTR3D?utm_source=chatgpt.com "GitHub - IceTTTb/PlaneTR3D: [ICCV'21] PlaneTR: Structure-Guided Transformers for 3D Plane Recovery · GitHub"
[7]: https://docs.opencv.org/4.x/d3/dc0/group__imgproc__shape.html?utm_source=chatgpt.com "OpenCV: Structural Analysis and Shape Descriptors"
[8]: https://github.com/cvg/DeepLSD?utm_source=chatgpt.com "GitHub - cvg/DeepLSD: Implementation of the paper \"DeepLSD: Line Segment Detection and Refinement with Deep Image Gradients\" · GitHub"
[9]: https://scikit-image.org/docs/stable/api/skimage.measure.html?highlight=label&utm_source=chatgpt.com "skimage.measure — skimage 0.26.0 documentation"
[10]: https://scikit-image.org/docs/0.23.x/auto_examples/edges/plot_skeleton.html?utm_source=chatgpt.com "Skeletonize — skimage 0.23.2 documentation"
[11]: https://github.com/OpenDriveLab/DetAny3D?utm_source=chatgpt.com "GitHub - OpenDriveLab/DetAny3D: [ICCV 2025] Detect Anything 3D in the Wild · GitHub"
[12]: https://github.com/allenai/WildDet3D?utm_source=chatgpt.com "GitHub - allenai/WildDet3D: Allen Institute for AI: WildDet3D: Scaling Promptable 3D Detection in the Wild · GitHub"
[13]: https://arxiv.org/abs/2207.10660?utm_source=chatgpt.com "Omni3D: A Large Benchmark and Model for 3D Object Detection in the Wild"
[14]: https://github.com/facebookresearch/sam-3d-objects?utm_source=chatgpt.com "GitHub - facebookresearch/sam-3d-objects: SAM 3D Objects · GitHub"
[15]: https://github.com/facebookresearch/sam-3d-objects/issues/56?utm_source=chatgpt.com "Errorness Transformation of Objects · Issue #56 · facebookresearch/sam-3d-objects · GitHub"
[16]: https://github.com/scikit-learn-contrib/hdbscan?utm_source=chatgpt.com "GitHub - scikit-learn-contrib/hdbscan: A high performance implementation of HDBSCAN clustering. · GitHub"
[17]: https://github.com/OpenMask3D/openmask3d?utm_source=chatgpt.com "GitHub - OpenMask3D/openmask3d · GitHub"
[18]: https://github.com/concept-graphs/concept-graphs?utm_source=chatgpt.com "GitHub - concept-graphs/concept-graphs: Official code release for ConceptGraphs · GitHub"
[19]: https://github.com/boschresearch/Open3DSG?utm_source=chatgpt.com "GitHub - boschresearch/Open3DSG: [CVPR 2024] Open3DSG: Open-Vocabulary 3D Scene Graphs from Point Clouds with Queryable Objects and Open-Set Relationships · GitHub"
[20]: https://github.com/open-mmlab/mmdetection3d?utm_source=chatgpt.com "GitHub - open-mmlab/mmdetection3d: OpenMMLab's next-generation platform for general 3D object detection. · GitHub"
