现在这一阶段按 **2 个常驻进程 + 1 个操作终端** 启动。API 负责 HTTP，主 Pipeline Worker 负责领取任务并自动拉起 SAM/MoGe 子进程；两个模型 Worker 不需要手动常驻启动。

## 先确认真实模式配置

```bash
cd /home/ai3d/src/scene-spatial-poc

grep -E \
'^(SCENE_FAKE_MODELS|SCENE_GPU_PHYSICAL_ID|SCENE_SAM_PYTHON|SCENE_MOGE_PYTHON|SCENE_SAM_WORKER|SCENE_MOGE_WORKER|SCENE_SAM_CHECKPOINT|SCENE_MOGE_CHECKPOINT)=' \
.env.server
```

重点应看到：

```text
SCENE_FAKE_MODELS=false
SCENE_GPU_PHYSICAL_ID=0
```

---

# 终端 1：启动 API

```bash
cd /home/ai3d/src/scene-spatial-poc

set -a
source .env.server
set +a

bash scripts/start_api.sh
```

保持终端不要关闭。

当前地址：

```text
http://127.0.0.1:8181
```

---

# 终端 2：启动主 Pipeline Worker

```bash
cd /home/ai3d/src/scene-spatial-poc

set -a
source .env.server
set +a

bash scripts/start_worker.sh
```

这个终端保持运行。

它会自动执行：

```text
领取任务
→ 启动真实 sam3_worker.py
→ Mask 后处理
→ 启动真实 moge2_worker.py
→ 空间求解
→ 输出 SpatialSceneObservation
```

模型进程是由主 Worker 临时启动的，不需要另外开 SAM 或 MoGe 服务。

---

# 终端 3：检查服务状态

```bash
curl -s http://127.0.0.1:8181/health
echo

curl -s http://127.0.0.1:8181/ready
echo
```

检查进程：

```bash
echo "===== API ====="
pgrep -af 'uvicorn.*scene_spatial'

echo
echo "===== Pipeline Worker ====="
pgrep -af 'scene_spatial.worker.main'
```

---

# 运行任务时的日志位置

任务运行后，先找最新任务：

```bash
LATEST_TASK=$(find /home/ai3d/outputs/scene-spatial \
  -maxdepth 1 -type d -name 'task_*' \
  | sort | tail -n 1)

echo "$LATEST_TASK"
```

查看模型日志：

```bash
tail -f "$LATEST_TASK/logs/sam.stderr.log"
```

另一个终端可看：

```bash
tail -f "$LATEST_TASK/logs/moge.stderr.log"
```

查看产物：

```bash
find "$LATEST_TASK" \
  -maxdepth 3 -type f \
  -printf '%p  %k KB\n' \
  | sort
```

最终重点文件：

```text
detection/detections.json
detection/masks/
geometry/points.npy
geometry/depth.npy
geometry/normals.npy
geometry/validity.npy
geometry/camera.json
spatial/spatial_scene_observation.json
evaluation/metrics.json
```

标准任务产物目录本来就按照 detection、geometry、spatial 等阶段组织。

---

# 停止整套服务

分别在 API 和主 Worker 终端按：

```text
Ctrl+C
```

不要手动长期启动：

```text
model_workers/sam3_worker.py
model_workers/moge2_worker.py
```

它们只在任务执行到对应阶段时，由主 Pipeline Worker 自动启动并在推理结束后退出。
